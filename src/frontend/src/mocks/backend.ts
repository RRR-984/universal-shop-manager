import { 
  AnalyticsPeriod,
  BillStatus,
  DateFormat,
  Language,
  NumberFormat,
  PaymentType,
  PriceType,
  ReturnStatus,
  ShopType,
  TaxSystem,
  Variant_ok_blocked,
 } from "../backend";
import type { backendInterface } from "../backend";

export const mockBackend: backendInterface = {
  isSetupComplete: async () => true,

  getShopConfig: async () => ({
    isSetupComplete: true,
    shopName: "Krishna Mobile Store",
    shopAddress: "123 Main Street, Mumbai, Maharashtra",
    shopPhone: "+91 9876543210",
    shopEmail: "krishna@example.com",
    country: "India",
    language: Language.English,
    currency: "INR",
    taxSystem: TaxSystem.GST,
    taxRate: 18,
    dateFormat: DateFormat.DDMMYYYY,
    numberFormat: NumberFormat.Indian,
    shopType: ShopType.Mobile,
    expiryAlertThresholdDays: BigInt(90),
    minStockAlertEnabled: true,
    deadStockAlertDays: BigInt(180),
  }),

  saveShopConfig: async (config) => config,
  updateShopConfig: async (config) => config,

  getSalesSummary: async (_shopId, period) => ({
    totalSales: 125000,
    totalProfit: 28500,
    billCount: BigInt(47),
  }),

  getTopProducts: async (_shopId, period, limit) => [
    { productId: BigInt(1), name: "iPhone 15 Pro 256GB Black", totalQty: 12, revenue: 180000, profit: 36000 },
    { productId: BigInt(2), name: "Samsung S24 128GB White", totalQty: 8, revenue: 96000, profit: 19200 },
    { productId: BigInt(3), name: "OnePlus 12 256GB Green", totalQty: 15, revenue: 90000, profit: 18000 },
    { productId: BigInt(4), name: "Redmi Note 13 Pro 128GB", totalQty: 22, revenue: 66000, profit: 11000 },
    { productId: BigInt(5), name: "Realme GT 5 256GB", totalQty: 9, revenue: 54000, profit: 9720 },
  ],

  getLowStockProducts: async (_shopId: string) => [
    { productId: BigInt(3), name: "OnePlus 12 256GB Green", stock: 2, minStock: 5 },
    { productId: BigInt(7), name: "Redmi Note 13 Pro 64GB", stock: 1, minStock: 3 },
  ],

  getNearExpiryProducts: async (_shopId: string) => [],
  getNearExpiryProductsByDays: async (_shopId: string, _days: bigint) => [],
  getDeadStockProducts: async (_shopId: string, _days: bigint) => [],

  getStockValue: async (_shopId: string) => 450000,

  getFastMovingProducts: async (_shopId: string, _limit: bigint) => [
    { productId: BigInt(4), name: "Redmi Note 13 Pro 128GB", totalQty: 22, revenue: 66000, profit: 11000 },
    { productId: BigInt(1), name: "iPhone 15 Pro 256GB Black", totalQty: 12, revenue: 180000, profit: 36000 },
    { productId: BigInt(3), name: "OnePlus 12 256GB Green", totalQty: 15, revenue: 90000, profit: 18000 },
  ],

  getSlowMovingProducts: async (_shopId: string, _limit: bigint) => [
    { productId: BigInt(6), name: "Motorola Edge 40 Pro", totalQty: 1, revenue: 34999, profit: 5000 },
    { productId: BigInt(8), name: "Nokia G42 5G 128GB", totalQty: 2, revenue: 20000, profit: 3000 },
  ],

  listProducts: async (filter) => [
    {
      id: BigInt(1),
      shopId: "",
      name: "iPhone 15 Pro 256GB Black",
      category: "Smartphone",
      unit: "NOS",
      stock: 10,
      minStock: 3,
      retailPrice: 134900,
      wholesalePrice: 130000,
      costPrice: 125000,
      isActive: true,
      shopType: ShopType.Mobile,
      createdAt: BigInt(Date.now() * 1000000),
      updatedAt: BigInt(Date.now() * 1000000),
      engineFields: {
        __kind__: "Mobile",
        Mobile: { brand: "Apple", model: "iPhone 15 Pro", storage: "256GB", color: "Black", imei: "123456789012345", ram: "8GB", mobileCategory: "Mobile", displaySize: "6.1", compatibility: "", accessoryType: "", serialNumber: "", warrantyMonths: BigInt(12), processor: "A17 Pro" },
      },
    },
    {
      id: BigInt(2),
      shopId: "",
      name: "Samsung S24 128GB White",
      category: "Smartphone",
      unit: "NOS",
      stock: 5,
      minStock: 2,
      retailPrice: 79999,
      wholesalePrice: 76000,
      costPrice: 72000,
      isActive: true,
      shopType: ShopType.Mobile,
      createdAt: BigInt(Date.now() * 1000000),
      updatedAt: BigInt(Date.now() * 1000000),
      engineFields: {
        __kind__: "Mobile",
        Mobile: { brand: "Samsung", model: "Galaxy S24", storage: "128GB", color: "White", imei: "987654321098765", ram: "8GB", mobileCategory: "Mobile", displaySize: "6.2", compatibility: "", accessoryType: "", serialNumber: "", warrantyMonths: BigInt(12), processor: "Snapdragon 8 Gen 3" },
      },
    },
    {
      id: BigInt(3),
      shopId: "",
      name: "OnePlus 12 256GB Green",
      category: "Smartphone",
      unit: "NOS",
      stock: 2,
      minStock: 5,
      retailPrice: 64999,
      wholesalePrice: 62000,
      costPrice: 58000,
      isActive: true,
      shopType: ShopType.Mobile,
      createdAt: BigInt(Date.now() * 1000000),
      updatedAt: BigInt(Date.now() * 1000000),
      engineFields: {
        __kind__: "Mobile",
        Mobile: { brand: "OnePlus", model: "12", storage: "256GB", color: "Flowy Emerald", imei: "111222333444555", ram: "12GB", mobileCategory: "Mobile", displaySize: "6.7", compatibility: "", accessoryType: "", serialNumber: "", warrantyMonths: BigInt(12), processor: "Snapdragon 8 Gen 3" },
      },
    },
  ],

  getProduct: async (id) => ({
    id,
    shopId: "",
    name: "iPhone 15 Pro 256GB Black",
    category: "Smartphone",
    unit: "NOS",
    stock: 10,
    minStock: 3,
    retailPrice: 134900,
    wholesalePrice: 130000,
    costPrice: 125000,
    isActive: true,
    shopType: ShopType.Mobile,
    createdAt: BigInt(Date.now() * 1000000),
    updatedAt: BigInt(Date.now() * 1000000),
    engineFields: {
      __kind__: "Mobile",
      Mobile: { brand: "Apple", model: "iPhone 15 Pro", storage: "256GB", color: "Black", imei: "123456789012345", ram: "8GB", mobileCategory: "Mobile", displaySize: "6.1", compatibility: "", accessoryType: "", serialNumber: "", warrantyMonths: BigInt(12), processor: "A17 Pro" },
    },
  }),

  createProduct: async (input) => ({
    id: BigInt(100),
    ...input,
    createdAt: BigInt(Date.now() * 1000000),
    updatedAt: BigInt(Date.now() * 1000000),
  }),

  updateProduct: async (input) => ({
    id: input.id,
    shopId: "",
    name: input.name,
    category: input.category,
    unit: input.unit,
    stock: input.stock,
    minStock: input.minStock,
    retailPrice: input.retailPrice,
    wholesalePrice: input.wholesalePrice,
    costPrice: input.costPrice,
    isActive: input.isActive,
    shopType: ShopType.Mobile,
    createdAt: BigInt(Date.now() * 1000000),
    updatedAt: BigInt(Date.now() * 1000000),
    engineFields: input.engineFields,
  }),

  deleteProduct: async () => true,

  searchProducts: async (_shopId: string, _term: string) => [],

  listBills: async (_shopId, filter) => [
    {
      id: BigInt(1),
      billNumber: BigInt(1001),
      shopId: _shopId,
      customerName: "Rahul Sharma",
      customerPhone: "+91 9123456789",
      status: BillStatus.Saved,
      priceType: PriceType.Retail,
      items: [
        { productId: BigInt(1), name: "iPhone 15 Pro 256GB Black", qty: 1, unit: "NOS", rate: 134900, discount: 0, taxAmt: 24282, lineTotal: 134900 },
      ],
      subtotal: 134900,
      totalDiscount: 0,
      taxSystem: TaxSystem.GST,
      taxRate: 18,
      taxBreakdown: { cgst: 12141, sgst: 12141, igst: 0 },
      extraCharges: [],
      grandTotal: 159182,
      profit: 9900,
      paymentType: PaymentType.full,
      amountPaid: 159182,
      amountPending: 0,
      createdAt: BigInt(Date.now() * 1000000),
    },
    {
      id: BigInt(2),
      billNumber: BigInt(1002),
      shopId: _shopId,
      customerName: "Priya Patel",
      customerPhone: "+91 9876543210",
      status: BillStatus.Saved,
      priceType: PriceType.Wholesale,
      items: [
        { productId: BigInt(2), name: "Samsung S24 128GB White", qty: 2, unit: "NOS", rate: 76000, discount: 0, taxAmt: 27360, lineTotal: 152000 },
      ],
      subtotal: 152000,
      totalDiscount: 0,
      taxSystem: TaxSystem.GST,
      taxRate: 18,
      taxBreakdown: { cgst: 13680, sgst: 13680, igst: 0 },
      extraCharges: [],
      grandTotal: 179360,
      profit: 8000,
      paymentType: PaymentType.partial,
      amountPaid: 100000,
      amountPending: 79360,
      createdAt: BigInt(Date.now() * 1000000),
    },
  ],

  getBill: async (id) => ({
    id,
    billNumber: BigInt(1001),
    shopId: "",
    customerName: "Rahul Sharma",
    customerPhone: "+91 9123456789",
    status: BillStatus.Saved,
    priceType: PriceType.Retail,
    items: [
      { productId: BigInt(1), name: "iPhone 15 Pro 256GB Black", qty: 1, unit: "NOS", rate: 134900, discount: 0, taxAmt: 24282, lineTotal: 134900 },
    ],
    subtotal: 134900,
    totalDiscount: 0,
    taxSystem: TaxSystem.GST,
    taxRate: 18,
    taxBreakdown: { cgst: 12141, sgst: 12141, igst: 0 },
    extraCharges: [],
    grandTotal: 159182,
    profit: 9900,
    paymentType: PaymentType.full,
    amountPaid: 159182,
    amountPending: 0,
    createdAt: BigInt(Date.now() * 1000000),
  }),

  createBill: async (_shopId, input) => ({
    id: BigInt(100),
    billNumber: BigInt(1099),
    shopId: _shopId,
    customerName: input.customerName,
    customerPhone: input.customerPhone,
    status: BillStatus.Saved,
    priceType: input.priceType,
    items: input.items,
    subtotal: input.items.reduce((s, i) => s + i.lineTotal, 0),
    totalDiscount: input.billDiscount,
    taxSystem: TaxSystem.GST,
    taxRate: 18,
    taxBreakdown: { cgst: 0, sgst: 0, igst: 0 },
    extraCharges: input.extraCharges,
    grandTotal: input.items.reduce((s, i) => s + i.lineTotal, 0),
    profit: 0,
    paymentType: input.paymentType,
    amountPaid: input.amountPaid,
    amountPending: Math.max(
      0,
      input.items.reduce((s, i) => s + i.lineTotal, 0) - input.amountPaid,
    ),
    createdAt: BigInt(Date.now() * 1000000),
  }),

  cancelBill: async () => true,

  getMetalRates: async () => ({
    gold: 6200,
    gold24k: 6200,
    gold22k: 5683,
    silver: 75,
    lastUpdated: BigInt(Date.now() * 1_000_000),
    available: true,
    isStale: false,
  }),

  refreshMetalRates: async () => undefined,

  setMetalRatesManual: async () => undefined,

  transformRatesResponse: async (input) => ({
    status: BigInt(200),
    body: input.response.body,
    headers: input.response.headers,
  }),

  // Admin methods
  initAdmin: async () => true,
  isAdminCaller: async () => false,
  recordUserLogin: async () => undefined,
  getAllUsers: async () => ({ __kind__: "ok" as const, ok: [] }),
  getUserDetails: async (_principal) => ({ __kind__: "ok" as const, ok: { principal: _principal, shopName: "Test Shop", shopType: "Mobile", firstSeen: BigInt(0), lastSeen: BigInt(0), loginCount: BigInt(1), isActive: true, isBlocked: false } }),
  addAdminNote: async () => ({ __kind__: "ok" as const, ok: BigInt(1) }),
  getAdminNotes: async () => ({ __kind__: "ok" as const, ok: [] }),
  deleteAdminNote: async () => ({ __kind__: "ok" as const, ok: null }),
  getAllShops: async () => ({ __kind__: "ok" as const, ok: [] }),
  setShopDisabled: async () => ({ __kind__: "ok" as const, ok: null }),
  adminDeleteShop: async () => ({ __kind__: "ok" as const, ok: null }),
  adminDeleteUser: async () => ({ __kind__: "ok" as const, ok: null }),
  adminBlockUser: async () => ({ __kind__: "ok" as const, ok: null }),
  checkIfBlocked: async () => false,
  loginCheck: async () => Variant_ok_blocked.ok,

  // Staff methods
  addStaff: async (_shopId, _staffPrincipal) => ({ __kind__: "ok" as const, ok: null }),
  removeStaff: async (_shopId, _staffPrincipal) => ({ __kind__: "ok" as const, ok: null }),
  getShopStaff: async (_shopId) => [],
  getMyRole: async (_shopId) => "owner" as unknown as import("../backend.d").ShopRole,
  // Customer methods
  searchCustomers: async (_shopId, _query) => [],
  createOrUpdateCustomer: async (_shopId, _name, _phone) => BigInt(1),
  getShopCustomers: async (_shopId) => [],
  getCustomerBills: async (_shopId, _customerId) => [],

  // Bill sharing methods
  generateBillShareToken: async () => null,
  getPublicBill: async () => null,

  // Payment methods
  getPendingPaymentBills: async (_shopId) => [],
  recordPayment: async (_shopId, _billId, _amount) => ({
    id: BigInt(1),
    billNumber: BigInt(1001),
    shopId: _shopId,
    customerName: "Test",
    customerPhone: "",
    status: BillStatus.Saved,
    priceType: PriceType.Retail,
    items: [],
    subtotal: 0,
    totalDiscount: 0,
    taxSystem: TaxSystem.Generic,
    taxRate: 0,
    taxBreakdown: { cgst: 0, sgst: 0, igst: 0 },
    extraCharges: [],
    grandTotal: 0,
    profit: 0,
    paymentType: PaymentType.full,
    amountPaid: 0,
    amountPending: 0,
    createdAt: BigInt(Date.now() * 1000000),
  }),
  recordReminderSent: async (_shopId, _billId) => undefined,

  getProductByBarcode: async (_shopId, _barcode) => null,

  // Supplier methods
  listSuppliersByShop: async (_shopId) => [],
  createSupplier: async (_shopId, name, businessType, phone) => ({
    id: `sup_${Date.now()}`,
    shopId: _shopId,
    name,
    businessType,
    phone,
  }),
  createSupplierPurchase: async (_shopId, supplierId, productId, quantity, purchasePrice, transportCharge) => ({
    id: `sp_${Date.now()}`,
    shopId: _shopId,
    supplierId,
    productId,
    quantity,
    purchasePrice,
    transportCharge,
    purchaseDate: BigInt(Date.now() * 1000000),
  }),
  updateSupplier: async (_supplierId, name, businessType, phone) => ({
    id: _supplierId,
    shopId: "",
    name,
    businessType,
    phone,
  }),
  deleteSupplier: async (_supplierId) => true,
  getDefaultCharges: async () => null,
  setDefaultCharges: async (_charges) => null,
  getSupplier: async (_supplierId) => null,
  listPurchasesByProduct: async (_shopId, _productId) => [],
  listPurchasesBySupplier: async (_shopId, _supplierId) => [],
  getLastNPurchasesForProduct: async (_shopId, _productId, _n) => [],
  // Returns & Store Credit
  createReturn: async (_shopId, input) => ({
    id: BigInt(1),
    originalBillId: input.originalBillId,
    shopId: _shopId,
    customerName: input.customerName,
    customerPhone: input.customerPhone,
    returnItems: input.returnItems,
    totalRefundAmount: input.returnItems.reduce((s, i) => s + i.lineTotal, 0),
    status: ReturnStatus.Pending,
    createdAt: BigInt(Date.now() * 1000000),
  }),
  approveReturn: async (_shopId, returnBillId) => ({
    id: returnBillId,
    originalBillId: BigInt(1),
    shopId: _shopId,
    customerName: "Test",
    customerPhone: "",
    returnItems: [],
    totalRefundAmount: 0,
    status: ReturnStatus.Approved,
    createdAt: BigInt(Date.now() * 1000000),
    approvedAt: BigInt(Date.now() * 1000000),
  }),
  rejectReturn: async (_shopId, input) => ({
    id: input.returnBillId,
    originalBillId: BigInt(1),
    shopId: _shopId,
    customerName: "Test",
    customerPhone: "",
    returnItems: [],
    totalRefundAmount: 0,
    status: ReturnStatus.Rejected,
    rejectionReason: input.reason,
    createdAt: BigInt(Date.now() * 1000000),
  }),
  listReturns: async (_shopId, _filter) => [],
  getReturnsByBill: async (_shopId, _billId) => [],
  getCustomerCredit: async (_shopId, _phone) => ({
    balance: 0,
    shopId: _shopId,
    customerId: _phone,
    transactions: [],
  }),
  applyStoreCredit: async (_shopId, _input) => ({
    balance: 0,
    shopId: _shopId,
    customerId: "",
    transactions: [],
  }),
};
